name: ego_local_database
description: Offline-first local persistence for the Ego Flutter client.
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  drift: ^2.18.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  path: ^1.9.0
  path_provider: ^2.1.4
  sqlcipher_flutter_libs: ^0.6.5

dev_dependencies:
  build_runner: ^2.4.12
  drift_dev: ^2.18.0
  flutter_test:
    sdk: flutter

